const CACHE='yurdunu-bil-v99.0.2';
const CORE=['/','/manifest.webmanifest','/icon-192.svg','/icon-512.svg'];
const fresh=req=>{const u=new URL(req.url);return u.pathname==='/sw.js'||req.destination==='document'||req.destination==='script'||req.destination==='style'||req.destination==='manifest'||u.searchParams.has('yb-force')||u.searchParams.has('yb-update');};
self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE)).then(()=>self.skipWaiting()))});
self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('yurdunu-bil-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',e=>{if(e.request.method!=='GET'||new URL(e.request.url).origin!==location.origin)return;if(new URL(e.request.url).pathname==='/sw.js')return;e.respondWith(fetch(e.request,{cache:fresh(e.request)?'no-store':'default'}).then(r=>{if(r.ok){const c=r.clone();caches.open(CACHE).then(x=>x.put(e.request,c)).catch(()=>{})}return r}).catch(()=>caches.match(e.request))) });
self.addEventListener('notificationclick',e=>{e.notification.close();const url=e.notification.data?.url||'/';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.focus();c.postMessage({type:'YB_NOTIFICATION_OPEN',url});return}}return clients.openWindow(url)}))});
self.addEventListener('push',e=>{let d={title:'Yurdunu Bil',body:'Bugünkü coğrafya çalışman seni bekliyor.',url:'/'};try{d={...d,...(e.data?.json()||{})}}catch{}e.waitUntil(self.registration.showNotification(d.title,{body:d.body,icon:'/icon-192.svg',badge:'/icon-192.svg',tag:d.tag||'yb-daily',renotify:false,data:{url:d.url||'/'}}))});