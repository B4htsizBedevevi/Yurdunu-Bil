const CACHE='yurdunu-bil-v61';
const CORE=['/','/manifest.webmanifest','/icon-192.svg','/icon-512.svg'];
const isFresh=req=>{const u=new URL(req.url);return u.pathname==='/sw.js'||req.destination==='document'||req.destination==='script'||req.destination==='style'||req.destination==='manifest'||u.searchParams.has('yb-force');};
self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE)).then(()=>self.skipWaiting()))});
self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('yurdunu-bil-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',e=>{if(e.request.method!=='GET'||new URL(e.request.url).origin!==location.origin)return;const u=new URL(e.request.url);if(u.pathname==='/sw.js')return;const fresh=isFresh(e.request);e.respondWith(fetch(e.request,{cache:fresh?'no-store':'default'}).then(r=>{if(r.ok){const copy=r.clone();caches.open(CACHE).then(c=>c.put(e.request,copy)).catch(()=>{})}return r}).catch(()=>caches.match(e.request))) });
self.addEventListener('notificationclick',e=>{e.notification.close();const url=e.notification.data?.url||'/';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const client of list){if('focus' in client){client.focus();client.postMessage({type:'YB_NOTIFICATION_OPEN',url});return}}return clients.openWindow(url)}))});
self.addEventListener('push',e=>{let d={title:'Yurdunu Bil',body:'Bugünkü coğrafya çalışman seni bekliyor.',url:'/'};try{d={...d,...(e.data?.json()||{})}}catch{}e.waitUntil(self.registration.showNotification(d.title,{body:d.body,icon:'/icon-192.svg',badge:'/icon-192.svg',tag:d.tag||'yb-daily',renotify:false,data:{url:d.url||'/'}}))});
