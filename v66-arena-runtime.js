/* Yurdunu Bil — Arena runtime v66: server-authoritative gameplay */
(()=>{'use strict';if(window.__YB66_ARENA__)return;window.__YB66_ARENA__=true;const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],C=window.YURDUNUBIL_CONFIG||{};let sb=null,user=null,match=null,me=null,opp=null,channel=null,poll=null,timer=null,answered=false,renderedRound=null;
try{if(window.supabase&&C.SUPABASE_URL&&C.SUPABASE_PUBLISHABLE_KEY)sb=window.supabase.createClient(C.SUPABASE_URL,C.SUPABASE_PUBLISHABLE_KEY)}catch{}
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]||c));
const toast=(m,t='ok')=>{if(window.showToast){window.showToast(m,t);return}const r=$('#toast-root');if(!r)return;const e=document.createElement('div');e.className=`toast ${t} show`;e.textContent=m;r.appendChild(e);setTimeout(()=>e.remove(),2200)};
const events=()=>{try{window.navigate?.('events')}catch{}};
async function auth(){if(!sb){toast('Arena bağlantısı hazır değil.','error');return null}const r=await sb.auth.getSession();user=r.data?.session?.user||null;if(!user)toast('Arena için giriş yapmalısın.','error');return user}
function cleanup(){if(timer){clearInterval(timer);timer=null}if(poll){clearInterval(poll);poll=null}if(channel){sb?.removeChannel(channel);channel=null}renderedRound=null}
async function players(){if(!match)return;const r=await sb.from('arena_players').select('*').eq('match_id',match.id).order('joined_at',{ascending:true});const list=r.data||[];me=list.find(x=>x.user_id===user.id)||null;opp=list.find(x=>x.user_id!==user.id)||null}
async function refresh(){if(!match)return null;const r=await sb.from('arena_matches').select('*').eq('id',match.id).maybeSingle();if(r.error){toast('Arena durumu alınamadı.','error');return null}if(r.data){match=r.data;await players();return match}return null}
function shell(html){const v=$('#view-events');if(v)v.innerHTML=`<div class="ybArena">${html}</div>`}
async function hub(){cleanup();match=me=opp=null;const u=await auth();if(!u)return;const r=await sb.from('arena_ratings').select('*').eq('user_id',u.id).maybeSingle();const d=r.data||{rating:1000,wins:0,losses:0,draws:0,best_streak:0};const league=d.rating>=1500?'Elit':d.rating>=1300?'Usta':d.rating>=1150?'Uzman':d.rating>=1000?'Yükselen':'Çaylak';shell(`<section class="ybArenaHero"><span class="ybArenaKicker">YURDUNU BİL • ARENA</span><h1>Bilgini rakibine karşı kanıtla.</h1><p>Server saatli 1 VS 1. Soru, süre ve puan sunucudan doğrulanır.</p><div class="ybArenaStats"><div><b>1 VS 1</b><small>Canlı düello</small></div><div><b>30</b><small>Sunucu sorusu</small></div><div><b>10</b><small>Tur</small></div><div><b>⚡</b><small>Hız puanı</small></div></div></section><section class="ybArenaGrid"><article class="ybArenaCard featured"><div class="ybArenaIcon">⚔️</div><span class="ybArenaTag">EN HIZLI</span><h3>Rakip Bul</h3><p>Bekleyen bir Arena odasına katıl veya yeni maç oluştur.</p><button class="btn primary" data-a="matchmake">Rakip Bul →</button></article><article class="ybArenaCard"><div class="ybArenaIcon">🔐</div><h3>Oda Oluştur</h3><p>Arkadaşınla paylaşabileceğin özel bir maç kodu oluştur.</p><div class="ybArenaModes"><button data-mode="duel" class="active">Klasik</button><button data-mode="speed">Hız</button><button data-mode="chain">Zincir</button></div><button class="btn secondary" data-a="create">Oda Kodu Oluştur</button></article><article class="ybArenaCard"><div class="ybArenaIcon">🎟️</div><h3>Koda Katıl</h3><p>6 haneli Arena kodunu gir.</p><div class="ybArenaJoin"><input id="ybArenaCode" maxlength="6" inputmode="text" placeholder="ABC123"><button class="btn secondary" data-a="join">Katıl</button></div></article></section><section class="ybArenaBoard"><div><span class="ybArenaKicker">LİG</span><h2>${league} • ${d.rating} RP</h2><p>🏆 ${d.wins} G • ${d.losses} M • 🔥 ${d.best_streak} seri</p></div></section>`;
$$('[data-mode]',view()).forEach(b=>b.onclick=()=>{$$('[data-mode]',view()).forEach(x=>x.classList.remove('active'));b.classList.add('active')});$$('[data-a]',view()).forEach(b=>b.onclick=()=>action(b.dataset.a));}
async function action(a){if(!user&&!(await auth()))return;const mode=$('[data-mode].active',view())?.dataset.mode||'duel';if(a==='create')return create(mode);if(a==='join')return join($('#ybArenaCode')?.value||'');if(a==='matchmake')return matchmake(mode)}
async function create(mode){const r=await sb.rpc('arena_create_match',{p_mode:mode,p_topic:null,p_question_count:10});if(r.error){toast(r.error.message,'error');return}match=r.data;await players();waiting();subscribe()}
async function join(code){code=String(code).trim().toUpperCase();if(code.length<4){toast('Arena kodunu gir.','error');return}const r=await sb.rpc('arena_join_match',{p_code:code});if(r.error){toast(r.error.message,'error');return}match=r.data;await players();if(match.status==='active')await game(true);else waiting();subscribe()}
async function matchmake(mode){shell(`<section class="ybArenaWait"><div class="ybArenaPulse">⚔️</div><span class="ybArenaKicker">EŞLEŞME ARANIYOR</span><h1>Rakip aranıyor…</h1><p>Yakındaki bekleyen bir Arena maçına güvenli şekilde bağlanıyoruz.</p><div class="ybArenaSearchLine"><i></i><i></i><i></i></div><button class="btn ghost" id="ybArenaCancel">Vazgeç</button></section>`);$('#ybArenaCancel').onclick=()=>{cleanup();hub()};const q=await sb.from('arena_matches').select('*').eq('mode',mode).eq('status','waiting').neq('created_by',user.id).order('created_at',{ascending:true}).limit(1);const c=q.data?.[0];if(c){const j=await sb.rpc('arena_join_match',{p_code:c.code});if(!j.error){match=j.data;await players();await game(true);subscribe();return}}await create(mode)}
function waiting(){shell(`<section class="ybArenaWait"><div class="ybArenaPulse">🛡️</div><span class="ybArenaKicker">ODA HAZIR</span><h1>Rakibini bekliyoruz.</h1><p>Kodu arkadaşına gönder. İki oyuncu hazır olduğunda maç otomatik başlar.</p><div class="ybArenaCode">${esc(match.code)}</div><button class="btn primary" id="ybArenaCopy">📋 Kodu Kopyala</button><button class="btn ghost" id="ybArenaCancel">İptal</button></section>`);$('#ybArenaCopy').onclick=async()=>{try{await navigator.clipboard.writeText(match.code);toast('Arena kodu kopyalandı.')}catch{toast(match.code)}};$('#ybArenaCancel').onclick=()=>{cleanup();hub()}}
function subscribe(){if(!match)return;if(channel)sb.removeChannel(channel);channel=sb.channel('yb66-arena-'+match.id).on('postgres_changes',{event:'UPDATE',schema:'public',table:'arena_matches',filter:'id=eq.'+match.id},async()=>{const old=match?.current_round;await refresh();if(match?.status==='finished'){finish();return}if(match?.status==='active'&&match.current_round!==old){await game(true)}else updateScore()}).on('postgres_changes',{event:'INSERT',schema:'public',table:'arena_players',filter:'match_id=eq.'+match.id},async()=>{await refresh();if(match.status==='waiting'&&opp)waiting();}).subscribe();poll=setInterval(async()=>{const oldRound=match?.current_round;await refresh();if(!match)return;if(match.status==='finished'){finish();return}if(match.status==='active'&&oldRound!==match.current_round){await game(true)}else updateScore()},2000)}
async function game(force=false){if(match?.status!=='active')return;await players();if(!force&&renderedRound===match.current_round)return;answered=false;renderedRound=match.current_round;const idx=match.current_round;const qr=await sb.rpc('arena_get_question',{p_match_id:match.id,p_question_index:idx});if(qr.error||!qr.data?.length){toast('Arena sorusu yüklenemedi.','error');return}const q=qr.data[0],opts=Array.isArray(q.options)?q.options:[];shell(`<section class="ybArenaTop"><div><span class="ybArenaKicker">${match.mode==='speed'?'HIZ ARENASI':match.mode==='chain'?'BİLGİ ZİNCİRİ':'KLASİK DÜELLO'}</span><h1>Canlı Düello</h1></div><div class="ybArenaRound" id="ybArenaQNum">Tur ${idx+1} / ${match.question_count}</div></section><section class="ybArenaScore"><div class="ybArenaPlayer me"><span>🧑‍💻</span><div><b>${esc(me?.display_name||'Sen')}</b><small>Sen • <strong id="ybArenaMeScore">${me?.score||0}</strong> puan</small></div></div><div class="ybArenaVS">VS</div><div class="ybArenaPlayer"><span>⚔️</span><div><b>${esc(opp?.display_name||'Rakip')}</b><small>Rakip • <strong id="ybArenaOppScore">${opp?.score||0}</strong> puan</small></div></div></section><section class="ybArenaQuestion"><div class="ybArenaQuestionMeta"><span id="ybArenaTimer">⏱</span><span>${esc(q.topic||'Genel')} • ${esc(q.difficulty||1)}/5</span></div><h2>${esc(q.question_text)}</h2><div class="ybArenaOptions">${opts.map((v,i)=>`<button data-answer="${i}"><b>${String.fromCharCode(65+i)}</b><span>${esc(v)}</span></button>`).join('')}</div><div class="ybArenaLive" id="ybArenaLive">İlk cevap sunucuya işlendiğinde rakibin durumu güncellenir.</div></section>`);updateScore();startTimer(match.mode==='speed'?15:25)}
function updateScore(){const a=$('#ybArenaMeScore'),b=$('#ybArenaOppScore');if(a)a.textContent=String(me?.score||0);if(b)b.textContent=String(opp?.score||0);const q=$('#ybArenaQNum');if(q&&match)q.textContent=`Tur ${Math.min(match.current_round+1,match.question_count)} / ${match.question_count}`}
function startTimer(seconds){if(timer)clearInterval(timer);const started=Date.parse(match.question_started_at||'');const getLeft=()=>{const elapsed=Number.isFinite(started)?(Date.now()-started)/1000:seconds;return Math.max(0,Math.ceil(seconds-elapsed))};const tick=()=>{const left=getLeft(),e=$('#ybArenaTimer');if(e)e.textContent='⏱ '+left+' sn';if(left<=0){clearInterval(timer);timer=null;if(!answered)submit(-1)}};tick();timer=setInterval(tick,250);$$('[data-answer]',view()).forEach(b=>b.onclick=()=>{if(answered)return;answered=true;$$('[data-answer]',view()).forEach(x=>x.disabled=true);submit(Number(b.dataset.answer))})}
async function submit(answerIndex){if(!match||!me)return;const r=await sb.rpc('arena_submit_answer',{p_match_id:match.id,p_player_id:me.id,p_question_index:match.current_round,p_answer_index:answerIndex,p_is_correct:false,p_response_ms:0,p_points:0});if(r.error){answered=false;$$('[data-answer]',view()).forEach(x=>x.disabled=false);toast(r.error.message==='stale_question'?'Tur değişti, yeni soru yükleniyor.':r.error.message,'error');await refresh();if(match?.status==='active')game(true);return}await refresh();const live=$('#ybArenaLive');if(live)live.textContent=r.data?.is_correct?'✓ Doğru! Sunucu puanı hesapladı.':'✕ Cevap işlendi. Sunucu skoru güncelliyor.';if(match?.status==='finished')finish();else if(renderedRound!==match.current_round)game(true)}
async function finish(){cleanup();await refresh();const mine=me?.score||0,theirs=opp?.score||0;const result=mine>theirs?'Zafer!':mine<theirs?'Rakip kazandı':'Berabere';const emoji=mine>theirs?'🏆':mine<theirs?'⚔️':'🤝';shell(`<section class="ybArenaFinish"><div class="ybArenaPulse">${emoji}</div><span class="ybArenaKicker">ARENA TAMAMLANDI</span><h1>${result}</h1><p>${esc(me?.display_name||'Sen')} <strong>${mine}</strong> — <strong>${theirs}</strong> ${esc(opp?.display_name||'Rakip')}</p><button class="btn primary" id="ybArenaAgain">Tekrar Oyna →</button><button class="btn ghost" id="ybArenaBack">Arena Merkezine Dön</button></section>`);$('#ybArenaAgain').onclick=()=>hub();$('#ybArenaBack').onclick=()=>hub()}
window.YBArena={open:async()=>{await auth();await hub()},close:()=>{cleanup();events()}};
})();
