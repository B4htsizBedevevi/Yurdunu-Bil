/* Compatibility shim.
 * The canonical question bank is data/questions.js.
 * This legacy loader is intentionally empty so older index references do not
 * create a 404 while the repository is being migrated to one source of truth.
 */
(()=>{'use strict';})();
